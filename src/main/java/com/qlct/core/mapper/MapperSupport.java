package com.qlct.core.mapper;

import jakarta.inject.Singleton;
import org.mapstruct.Named;

import java.util.Date;

@Singleton
@Named("MapperSupport")
public class MapperSupport {

    public Date timestampToDate(Date timestamp) {

        return null;
    }
}
